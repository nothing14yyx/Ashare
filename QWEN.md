# AI 助手入口指南

> **核心指令**：请使用 **中文（简体）** 回答。

**⚠️ 重要**：在执行任何任务前，请**务必先阅读**主控文档：
👉 [`docs/AI_CONTEXT.md`](docs/AI_CONTEXT.md)

该文档包含：
1.  **强制工具**：如何使用现成的 `raw_reader.py` 和 `db_query.py`（**严禁**编写临时脚本）。
2.  **项目架构**：代码结构与数据库设计。
3.  **运行指南**：正确的脚本启动命令。